export interface Vehicle {
  name: string;
  cargo_capacity: string;
  model: string;
  passengers: string;
  manufacturer: string;
  url: string;
}

export type VehiculeImages = {
  [key: string]: string;
};
